spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.137.128:3306/mall_pms
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto